{
  "name": "jwe-codex",
  "version": "1.0.0",
  "description": "Jurassic World Evolution Codex mobile app",
  "scripts": {
    "build:ios": "npx cap add ios && npx cap sync ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/cli": "^6.0.0"
  }
}
